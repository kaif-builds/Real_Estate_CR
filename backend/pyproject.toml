[project]
name = "realestate-crm-backend"
version = "0.1.0"
description = "RealEstateCRM Backend API"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
